/**
 * Toast/index.jsx
 * Re-exports the useToast hook and ToastProvider for convenience.
 * The actual toast rendering is handled inside ToastContext.
 */
export { ToastProvider, useToast } from '../../context/ToastContext'
